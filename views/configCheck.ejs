<!DOCTYPE html>
<html>
<head>
    <% include partials/master/header %>
</head>
<body class="white-bg">

<div id="wrapper">
    <div class="wrapper embedded-content">
        <div class="row">
            <div class="col-lg-12" style="height:1200px">
                <h1>Config Check</h1>
                <table class="table">
                    <tr>
                        <td>Package version</td>
                        <td><%- packageInfo.version %></td>
                    </tr>

                    <tr>
                        <td>Project Path</td>
                        <td><%- proc.cwd() %></td>
                    </tr>

                    <tr>
                        <td>NODE_ENV</td>
                        <td><%- proc.env.NODE_ENV %></td>
                    </tr>

                    <tr>
                        <td>NODE_PATH</td>
                        <td><%- proc.env.NODE_PATH %></td>
                    </tr>

                    <% for (let key in versions) { %>
                    <tr>
                        <td><%- key %></td>
                        <td><%- versions[key] %></td>
                    </tr>
                    <% } %>
                </table>
            </div>
        </div>
    </div>
</div>

<% include partials/master/footerScripts %>

</body>
</html>
