<!DOCTYPE html>
<html>
<head>
    <% include partials/master/header %>
</head>
<body class="fixed-navigation">

<div id="wrapper">

    <% include partials/master/leftPanel %>

    <div id="page-wrapper" class="white-bg">

        <% include partials/master/topPanel %>

        <div class="wrapper wrapper-content">

            <% include partials/master/message %>

            <div class="row white-bg">
                <div class="col-lg-12">
                    <h1 id="title">Link Project</h1>
                    Existing Hitslink customers can create free projects that will duplicate tracking in both existing Hitslink accounts and the new version.  There is no charge for this, and
                    nothing changes on your existing Hitslink account.
                    <br><br>
                    To create a free project, enter your Hitslink account's login credentials to link the account.  Each account (and sub-account) must be linked
                    separately.  Once linked, your stats will be tracked in both products.
                    <br /><br />

                    <form role="form">
                        <div class="form-group"><label>Hitslink Account Name</label>
                            <input name="legacyName" id="legacyName" placeholder="Enter the existing Hitslink account name..." class="form-control" autocomplete="off">
                        </div>
                        <div class="form-group"><label>Hitslink Password</label>
                            <input type="password" class="form-control" placeholder="Password" name="password" id="password" required="">
                        </div>
                        <div>
                            <button onclick="submitForm(); return false" class="btn btn-sm btn-primary m-t-n-xs" type="submit"><strong id="projectSubmit">Link</strong></button>
                            <br><br><br><br><br><br><br>
                        </div>
                    </form>
                    <br /><br />
                </div>
            </div>
        </div>

        <% include partials/master/footer %>
    </div>
</div>

<% include partials/master/footerScripts %>

<script>
    function getFormData() {
        var params = {
            account: $('#legacyName').val(),
            password: $('#password').val()
        };

        return params;
    }

    function submitForm() {

        var val = $('#legacyName').val();

        if (!val) {
            Page.showMessage('You must enter a name for the account.')
            return;
        }

        //  validation passed, update the data
        Data.submitForm('/setup/projects/link', getFormData(), function() {
            window.location = '/setup/projects'
        });
    }
</script>
</body>
</html>
