The TCP Port monitor checks if a server is accepting TCP connection requests on a specific I.P. address port.  No commands are sent, and the connection is
immediately closed.  This is used as a way to determine if services are accessible.
